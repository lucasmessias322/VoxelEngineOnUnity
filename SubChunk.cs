// Legacy placeholder kept so generated project files that still reference this path
// continue compiling until Unity regenerates the .csproj. Logical subchunk state now
// lives inside Chunk.SubchunkState and no longer uses a MonoBehaviour/GameObject.
